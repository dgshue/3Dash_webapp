name: "3Dash"
version: "1.3.0"
slug: "3dash"
description: "3D Dashboard for Home Assistant — BLE positioning fork with calibration"
url: "https://github.com/dgshue/3Dash_webapp"
arch:
  - amd64
  - aarch64
  - armv7
init: false
startup: "application"
boot: "auto"

# Phase 6: HA ingress. The Companion App refuses self-signed certs, so
# serving 3Dash via HA's ingress is the only path that works on iOS.
# `ingress_port: 8099` tells HA's ingress proxy which internal port nginx
# is on. We drop the host-port mapping entirely — no more 8099/tcp on the
# HA host.
ingress: true
ingress_port: 8099

# Sidebar integration so 3Dash shows up in HA's left nav.
panel_icon: "mdi:floor-plan"
panel_title: "3Dash"
panel_admin: false

options: {}
schema: {}
